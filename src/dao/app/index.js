// Donde vamos a generar la instacia de mi clase
import ProductsManager from "./ProductsManager.js";
import PATH from "../../consts/PATH.js";

const NewManager = new ProductsManager(PATH);

export default NewManager;
